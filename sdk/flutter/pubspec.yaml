name: laligence_ekyc_sdk
description: Flutter SDK for the LALIGENCE eKYC backend API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.6.0
  http_parser: ^4.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
